# Ajax Directory
This folder holds asynchronous endpoint scripts (e.g., form handlers, validation checks, live UI updates).
Currently, the admin dashboard handles AJAX-friendly request flows directly via standard Controller routes.
